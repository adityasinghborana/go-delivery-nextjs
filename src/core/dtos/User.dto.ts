export interface UserDto {
  name: string;
  email: string;
  phone: string;
  firebaseUid: string;
}
export interface ProfileDto {
  name: string;
  firstName?: string;
  lastName?: string;
  Dob?: Date;
  email: string;
  phone: string;
}
